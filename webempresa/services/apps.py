from django.apps import AppConfig


class ServicesConfig(AppConfig):
    name = 'services'
    verbose_name = 'Gestor de servicios' # cambio el nombre de la app, este nombre se muestra en el panel del admin
